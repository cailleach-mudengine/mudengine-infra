name: 'OnDestroy'

on:  
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  starter-ecs-dev-publish:
    uses: cailleach-devops/aws-shared-actions/.github/workflows/aws-ecs-destroy.yml@tf-actions
    with:
      environment: "dev"
    secrets: inherit
