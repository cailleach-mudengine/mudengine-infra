name: 'OnPullRequest'

on:  
  pull_request:
    types: [opened, reopened]
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  starter-ecs-pull-request:
    uses: cailleach-devops/aws-shared-actions/.github/workflows/aws-ecs-pull-request.yml@tf-actions
    with:
      environment: 'dev'
    secrets: inherit
